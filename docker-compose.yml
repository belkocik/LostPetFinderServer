services:
  auth:
    build:
      context: .
      dockerfile: ./apps/auth/Dockerfile
      target: development
    command: pnpm run start:dev auth
    ports:
      - '3000:3000'
    volumes:
      - .:/usr/src/app

  postgres:
    image: postgres:13.1-alpine
    ports:
      - '5432:5432'
